# Ultimate NetGuard AIO — قائمة المميزات الكاملة

> **الإصدار:** V1.2.0  
> **المطور:** علي سكاف (Ali Sakkaf)  
> **الموقع:** [alisakkaf.com](https://alisakkaf.com)  
> **GitHub:** [github.com/alisakkaf](https://github.com/alisakkaf)

---

## 🌐 الموديول 1: مراقب حركة الشبكة في الوقت الحقيقي

### محرك الالتقاط الأساسي
- **التقاط الحزم الخام (Raw Socket)** — يستخدم مقابس Windows الخام `SIO_RCVALL` لمراقبة حقيقية على مستوى الحزم بدون WinPcap أو Npcap
- **الدعم المزدوج IPv4/IPv6** — التقاط متزامن على مقبسي IPv4 و IPv6 في نفس الوقت
- **اختيار كرت الشبكة التلقائي الذكي (Smart Auto-Detect Engine)** — خوارزمية ذكية متقدمة تفحص حجم الترافيك الفعلي (`InOctets + OutOctets`) وتستعلم جداول التوجيه (`GetBestInterface` لـ IPv4 و `GetBestRoute2` لـ IPv6) لاختيار كرت الإنترنت الحقيقي تلقائياً عند بدء التشغيل وعند تغير الاتصالات (WiFi / Ethernet / VPN)
- **تحديث تلقائي لكروت الشبكة** — الضغط على القائمة المنسدلة يكتشف فوراً أي اتصال جديد
- **تصفية الكروت الوهمية والافتراضية** — استبعاد تلقائي ذكي لجميع كروت الشبكة الافتراضية والـ VPN الوهمية (VMware, VirtualBox, Radmin, ZeroTier, Hamachi, TAP/TUN, WSL, vEthernet)
- **خيوط عالية الأداء** — خيط التقاط مخصص بأولوية عالية (`QThread::HighPriority`) مع مخزن مؤقت 32 ميجابايت
- **المعالجة الجماعية** — تجميع الحزم (250 مللي ثانية أو 3000 حزمة) لتقليل الحمل على واجهة المستخدم
- **إيقاف قسري للالتقاط** — إيقاف سلس مع مهلة ثانيتين، ينتقل لإنهاء قسري للخيط إذا لم يستجب
- **استعادة صامتة من أخطاء الـ Socket** — إعادة تشغيل تلقائية للبرنامج في حال خطأ ربط المقبس

### تتبع حركة المرور لكل عملية وتصنيف الرفع/التحميل
- **تحليل PID في الوقت الحقيقي** — ربط كل حزمة بالعملية المالكة لها عبر `GetExtendedTcpTable` / `GetExtendedUdpTable`
- **تمبيز دقيق 100% بين التحميل والرفع (Upload / Download Precision)** — معالجة متقدمة تضمن تصنيف اتجاه حركة البيانات بدقة 100% حتى أثناء ضغط الحزم المفاجئ وحركة الشبكات المحلية والـ VPN
- **ربط عمليات IPv6** — دعم كامل لجداول TCP/UDP الخاصة بـ IPv6
- **محلل خدمات Windows المتعددة (Svchost Multi-Service Resolver)** — التعرف على كافة أسماء خدمات Windows العاملة تحت نفس PID لعملية `svchost.exe` ودمج أسماء المعروضات بعلامة `+` (مثل: `Windows Update + Background Intelligent Transfer Service`)
- **تخزين مؤقت ذكي لحالة TCP** — يتعامل مع حالات LISTEN و ESTABLISHED وربط المنافذ العامة (0.0.0.0)
- **خيط تخزين مؤقت في الخلفية** — خيط منفصل يحدّث ذاكرة التخزين المؤقت كل 200 مللي ثانية للتجاوب اللحظي

### شجرة عرض الشبكة
- **نموذج بيانات مخصص QAbstractItemModel** — نموذج شجري هرمي: عقد العمليات ← أبناء الاتصالات
- **8 أعمدة بيانات** — التطبيق/البروتوكول، المصدر، الوجهة، الخدمة، سرعة التنزيل، سرعة الرفع، إجمالي البايتات، الحزم
- **حساب السرعة الحية مع تنعيم EMA** — حساب السرعة بالثانية وتنعيم التذبذبات اللحظية عبر خوارزمية Exponential Moving Average
- **الفرز الحي** — فرز رقمي دقيق عبر `UserRole+5` و `QSortFilterProxyModel`
- **البحث/التصفية** — تصفية تكرارية فورية عبر جميع الأعمدة
- **توسيع/طي الكل** — زر تبديل لتوسيع الشجرة
- **محرك استخراج الأيقونات الأصلي (100% Icon Engine)** — استخراج أيقونات التطبيقات الحقيقية عبر `SHGetFileInfoW` (`SLGP_UNCPRIORITY`) و `QtWin::fromHICON` وتوسيع متغيرات البيئة ورجوع `System32` حتى للخدمات والتطبيقات مغلقة المصدر
- **صلاحيات التصحيح** — تفعيل `SeDebugPrivilege` للوصول لأيقونات عمليات النظام والخدمات المحمية

### القائمة المنسدلة (كليك يمين)
- **إنهاء العملية** — إنهاء إجباري لأي عملية عبر PID
- **نافذة خصائص العملية** — حوار احترافي يعرض:
  - أيقونة العملية بحجم 150×150
  - اسم العملية، PID، النوع (تطبيق/خدمة/نظام)
  - المسار الكامل للملف (نص قابل للتحديد)
  - إحصائيات الترافيك: سرعة التنزيل/الرفع، الاستهلاك الكلي، عدد الحزم
  - زر "فتح موقع الملف" (يفتح المستكشف مع تحديد الملف)
- **نسخ تفاصيل الاتصال** — نسخ معلومات المصدر:المنفذ للحافظة

### بطاقات لوحة المعلومات
- **عداد الحزم** — إجمالي الحزم الملتقطة
- **سرعة التنزيل** — سرعة RX فورية مع تدرج تلقائي (B/s → KB/s → MB/s)
- **سرعة الرفع** — سرعة TX فورية مع تدرج تلقائي
- **سرعة الهاردوير** — السرعة على مستوى النظام عبر عدادات كرت الشبكة الحقيقية (`GetIfEntry2`)

### البروتوكولات المدعومة
- TCP, UDP, ICMP, ICMPv6, TCP6, UDP6
- خريطة الخدمات: HTTP, HTTPS, DNS, SSH, FTP, SMTP, IMAP, POP3, MySQL, PostgreSQL, Redis, MongoDB, RDP, VNC, NTP, NetBIOS, SMB, DHCP, IKE, OpenVPN, والمزيد

---

## 🛡️ الموديول 2: مدير جدار الحماية (Firewall)

### تكامل Windows COM API
- **واجهة INetFwPolicy2 المباشرة** — تكامل COM أصلي مع جدار حماية Windows (بدون netsh)
- **مجموعة INetFwRules** — عمليات CRUD كاملة على قواعد جدار الحماية
- **بديل Netsh** — انتقال تلقائي لـ `netsh advfirewall` للحالات الخاصة

### إدارة القواعد
- **حظر التطبيق** — إنشاء قواعد حظر مزدوجة (واردة + صادرة)
- **سماح التطبيق** — إنشاء قواعد سماح مزدوجة (واردة + صادرة)
- **إزالة القواعد** — حذف القواعد بالاسم أو مسار التطبيق
- **تفعيل/تعطيل** — تفعيل أو تعطيل أي قاعدة بدون حذفها
- **تحديد متعدد** — تحديد عدة قواعد مع اختصارات Ctrl+A و Delete
- **تسمية موحدة** — نظام تسمية `NetGuard_<اسم_التطبيق>_<الإجراء>_<الاتجاه>`

### حل الاختصارات والأيقونات الدقيق
- **دعم وترجمة ملفات .lnk** — حل اختصارات Windows واستخراج مسار الملف التنفيذي الأصلي (`.exe`) عبر `IShellLinkW` بوضع `SLGP_UNCPRIORITY`
- **استخراج أيقونات قواعد النظام 100%** — قراءة واستخراج أيقونات التطبيقات وقواعد جدار الحماية حتى للمسارات التي تحتوي على متغيرات بيئية (`%SystemRoot%`) أو ملفات النظام
- **تنقية المسار** — تحويل صارم للفواصل المائلة الخلفية لتوافق COM API

### وضع القائمة البيضاء (الإغلاق الكامل)
- **إقفال الشبكة الكامل** — حظر كل حركة المرور الصادرة عبر جميع الملفات الشخصية
- **تعطيل القواعد المتداخلة** — تعطيل جميع قواعد السماح غير التابعة لـ NetGuard
- **حماية الخدمات الحرجة** — سماح تلقائي لـ `svchost.exe` و `System` لمنع انهيار النظام
- **تبديل بنقرة واحدة** — تفعيل/تعطيل مع حوار تأكيد تحذيري

### واجهة ثنائية التبويب
- **تبويب NetGuard الأساسي** — عرض القواعد المُنشأة بواسطة البرنامج فقط
- **تبويب قواعد النظام** — متصفح كامل لقواعد جدار حماية Windows
- **مفاتيح الاتجاه** — أزرار راديو للواردة/الصادرة في كل تبويب
- **بحث/تصفية** — تصفية فورية عبر جميع الأعمدة

### السحب والإفلات
- **إسقاط ملفات EXE و .lnk** — اسحب أي ملف .exe أو اختصار .lnk على تبويب الجدار الناري للحظر/السماح
- **حوار الإجراء** — اختر حظر، سماح، أو إلغاء عند الإسقاط

### الاستيراد / التصدير
- **تصدير JSON** — تصدير جميع قواعد NetGuard إلى ملف JSON
- **استيراد JSON** — استيراد وإعادة إنشاء القواعد من نسخة JSON احتياطية

### ملفات قواعد جدار الحماية الجاهزة (6 ملفات JSON)
- **🏢 الدرع العام لبيئة العمل** — للمكاتب: المتصفحات والاجتماعات المرئية والتخزين السحابي مسموح؛ التطبيقات الخلفية محظورة
- **🚀 بطولة الرياضات الإلكترونية** — للاعبين المحترفين: أقل بنق بحظر التحديثات؛ منصات الألعاب والمحادثة الصوتية والبث المباشر مسموح
- **💻 صندوق رمال المطوّرين** — لمهندسي البرمجيات: اتصال مطلق لـ IDEs و Docker وقواعد البيانات ومساعدي الذكاء الاصطناعي
- **🔒 حصن الخصوصية المطلقة** — أقصى حماية ضد التتبع: فقط متصفحات Tor/Brave وVPN والمراسلة المشفرة
- **📥 طليعة التحميل والميديا** — للأجهزة المخصصة: فقط مدراء التحميل وعملاء التورنت ومشغلات الميديا
- **🚫 جدار العزل وإيقاف التتبع** — يُجبر أدوبي وأوتوديسك وكوريل على الأوفلاين للحفاظ على الرخص

### نموذج جدول الجدار الناري
- **6 أعمدة** — التطبيق، اسم القاعدة، الإجراء، البروتوكول، الحالة، النوع
- **أيقونات التطبيقات الحقيقية** — دعم `QFileIconProvider` و `SHGetFileInfoW` لإظهار أيقونات جميع البرامج وقواعد النظام
- **ترميز الألوان** — أخضر للسماح، أحمر للحظر، ذهبي لقواعد NetGuard
- **متوافق مع الثيم** — الألوان تتكيف مع الوضع الداكن/فاتح

---

## 💻 الموديول 3: مراقب أجهزة النظام (Hardware)

### مصادر البيانات
- **حمل المعالج** — عداد PDH: `\Processor(_Total)\% Processor Time`
- **نشاط القرص** — عداد PDH: `\PhysicalDisk(_Total)\% Disk Time`
- **استخدام الذاكرة** — واجهة `GlobalMemoryStatusEx` (مستخدم/إجمالي/نسبة)
- **حمل كرت الشاشة** — WMI: `Win32_PerfFormattedData_GPUPerformanceCounters_GPUEngine` (محرك 3D)
- **الشبكة I/O** — واجهة `GetIfTable` (جميع الواجهات غير اللوبباك)

### مراقبة كروت الشاشة المتعددة (Multi-GPU DXGI)
- **اكتشاف كروت الشاشة الحقيقية عبر DXGI** — استخدام `CreateDXGIFactory` و `IDXGIFactory::EnumAdapters` لجلب أسماء جميع كروت الشاشة المثبتة (مثل NVIDIA GeForce RTX, Intel UHD Graphics, AMD Radeon)
- **تصفية WMI المتقدمة** — فلترة العدادات حسب المعرف الفيزيائي لكل كرت شاشة

### مراقبة درجة الحرارة
- **حرارة المعالج** — مصادر متعددة مع بدائل تتالية:
  1. `MSAcpi_ThermalZoneTemperature` (ROOT\WMI) — تحويل من كلفن
  2. `Win32_TemperatureProbe` (CIMV2) — مئوية مباشرة
  3. `Win32_PerfFormattedData_Counters_ThermalZoneInformation` — هجين
- **حرارة القرص** — `MSFT_StorageReliabilityCounter` (ROOT\Microsoft\Windows\Storage)
- **حرارة كرت الشاشة** — استعلامات WMI لمستشعرات GPU
- **حرارة اللوحة الأم** — مستخرجة من مناطق ACPI الحرارية (أدنى قراءة مستشعر)

### بنية WMI
- **3 مساحات أسماء WMI** — `ROOT\CIMV2`، `ROOT\WMI`، `ROOT\Microsoft\Windows\Storage`
- **تحليل Variant قوي** — يتعامل مع جميع أنواع البيانات (أعداد صحيحة، عشرية، نصوص)
- **جمع بيانات مترابط** — خيط خلفي `QThread` مع فترة استطلاع 1 ثانية

### عناصر واجهة مخصصة
- **مقياس دائري (CircularGauge)** — رسم قوس دائري مخصص مع:
  - قوس القيمة (أزرق < 60%، كهرماني < 85%، أحمر ≥ 85%)
  - نص النسبة المئوية المركزي (18pt عريض)
  - نص فرعي لدرجة الحرارة
  - تسمية الجهاز
- **شريط الحرارة (TempBar)** — شريط حرارة أفقي بألوان متدرجة
- **رسم متوافق مع الثيم** — يستخدم `QPalette` للتكيف التلقائي مع الداكن/الفاتح

---

## 📊 الموديول 4: سجل استخدام الشبكة

### محرك البيانات الذكي
- **تتبع لكل تطبيق** — تسجيل بايتات التنزيل/الرفع لكل تطبيق لكل يوم
- **استخراج حي** — قراءة البيانات مباشرة من NetworkTreeModel كل ثانية
- **تخزين مسار التطبيق (`exePath`)** — حفظ المسار الكامل للتطبيقات في السجل لضمان إظهار أيقوناتها الأصلية دائماً
- **حفظ بتنسيق JSON** — حفظ تلقائي في `%AppData%/NetGuard_History.json`
- **حفظ تلقائي** — كتابة على القرص كل 60 ثانية أثناء وجود حركة مرور نشطة
- **حفظ عند الخروج** — ضمان حفظ البيانات عند إغلاق التطبيق

### واجهة شجرة السجل
- **4 فلاتر زمنية** — اليوم، آخر 7 أيام، هذا الشهر، كل الأوقات
- **وضع اليوم** — قائمة مسطحة مرتبة حسب إجمالي الترافيك (تنازلي)
- **وضع الأيام المتعددة** — شجرة هرمية مجمعة حسب التاريخ ← التطبيقات
- **بطاقات الملخص** — إجمالي التنزيل، إجمالي الرفع، أعلى تطبيق مع الأيقونة
- **حفظ حالة التوسيع** — تذكر أي عقد التاريخ كانت متوسعة أثناء التحديث
- **استعادة الأيقونات 100% للبرامج المغلقة** — دالة `getOrResolveIcon` تجلب وتظهر أيقونة البرنامج الأصلية في السجل حتى ولو كان البرنامج غير شغال حالياً أو بعد إعادة تشغيل الجهاز

### إدارة البيانات
- **تصدير CSV** — تصدير بيانات السجل المرئية بتنسيق أب/ابن
- **مسح الكل** — حذف جميع السجلات مع حوار تأكيد
- **أمان الخيوط** — تحديث واجهة مبني على مؤقت فقط عند تفعيل تبويب السجل

---

## ⚙️ الموديول 5: الإعدادات والتكوين

### الإعدادات العامة
- **التشغيل عند بدء النظام** — تسجيل سجل Windows للتشغيل التلقائي
- **البدء مصغراً** — التشغيل مباشرة في علبة النظام
- **تبديل الثيم الداكن/الفاتح** — زر شمس/قمر متحرك مع تبديل QSS كامل
- **حفظ الإعدادات** — `QSettings` (سجل Windows) لجميع التفضيلات

### تخصيص طبقة شريط المهام (Taskbar Overlay Customization)
- **تفعيل/تعطيل الطبقة** — تبديل عنصر السرعة في شريط المهام
- **تبديل وحدات الهاردوير** — خيارات مستقلة لعرض: المعالج (CPU)، الرام (RAM)، كرت الشاشة (GPU)، ودرجات الحرارة
- **صيغ عرض الرام** — اختيار صيغة العرض (نسبة %، MB، أو GB)
- **اختيار كرت الشاشة** — قائمة منسدلة لاختيار الكرت المُراد مراقبته (تلقائي أو كرت محدد)
- **حجم الخط** — قابل للتعديل من 7 إلى 16 نقطة
- **شفافية الخلفية** — نطاق 0-255
- **لون النص** — أبيض، أسود، أخضر، أزرق، أصفر
- **لون الخلفية** — زجاج داكن، أسود صلب، أزرق صلب، شفاف

---

## 🖥️ الموديول 6: طبقة شريط المهام (Taskbar Overlay 2.0)

### البنية والظهور الدائم فوق كل النوافذ (Top-Most)
- **عنصر شفاف بدون إطار** — نافذة أدوات شفافة تعلق بشريط المهام
- **بدون تنشيط** — لا يسرق التركيز أبداً من النوافذ الأخرى (`WS_EX_NOACTIVATE | WS_EX_TOOLWINDOW`)
- **ملتصق بشريط المهام** — يُلصق كنافذة ابن لـ `Shell_TrayWnd`
- **ظهور دائم فوق كل النوافذ (Top-Most)** — تفعيل `Qt::WindowStaysOnTopHint` و `SetWindowPos(HWND_TOPMOST)` لضمان الرؤية الدائمة فوق كافة النوافذ والبرامج
- **نجاة من إعادة تشغيل Explorer** — يكتشف فقدان الأب ويعيد الربط تلقائياً
- **دعم شريط المهام المخفي تلقائياً** — يخفي نفسه عند ارتفاع شريط المهام < 10px

### التحريك والموقع الحر (Free-Form Positioning Anywhere)
- **تغيير الموضع والمكان في أي مكان** — إمكانية سحب وتحريك الويدجت بالماوس ووضعه في أي مكان على سطح المكتب أو شريط المهام
- **حفظ الموضع** — حفظ الإحداثيات عبر إعادة التشغيل وتغيير دقة الشاشة وتكبير DPI
- **إعادة التوضيع بنقرة واحدة (Reset Position)** — خيار `🔄 Reset Position` في قائمة الكليك اليمين لإعادة الويدجت فوراً لموقعه الافتراضي بجانب علبة النظام

### تغيير كرت الشبكة بنقرة واحدة من التاسك بار
- **قائمة فرعية اختيار كرت الشبكة (`🌐 Select Network Adapter`)** — تظهر عند الضغط كليك يمين على الويدجت، تحتوي على خيار التحديد التلقائي الذكي وكافة الكروت المتاحة لتبديل الشبكة مباشرة بدون فتح النافذة الرئيسية

### عرض السرعة الحية ومقاييس الهاردوير
- **أسهم التحميل/الرفع** — بألوان مميزة (رفع: كهرماني، تنزيل: أزرق)
- **تدرج تلقائي** — B/s → KB/s → MB/s
- **مؤشرات الهاردوير** — عرض CPU %, RAM (GB/MB/%), GPU %, والحرارة (°CPU/GPU)
- **ظلال نصوص واقية** — `QGraphicsDropShadowEffect` لضمان قراءة النصوص بنسبة 100% فوق أي خلفية

### النافذة المنبثقة للإحصائيات (عند التمرير)
- **10 مقاييس حية** — سرعة التنزيل/الرفع، إجمالي الجلسة تنزيل/رفع، حمل المعالج والرام، تفاصيل GPU والرام الخاصة به، درجات حرارة المعالج وكرت الشاشة
- **متوافقة مع الثيم** — نافذة منبثقة بنمط تلميح داكن
- **بدون تركيز** — أعلام نافذة `Qt::ToolTip` تمنع التنشيط

### قائمة الكليك اليمين
- **اختيار كرت الشبكة** — تغيير الكرت المباشر أو التحديد التلقائي
- **تكبير/تصغير الحجم** — تغيير حجم الخط ديناميكياً
- **إعادة التوضيع** — إعادة الموضع الافتراضي (Reset Position)
- **إظهار/تصغير التطبيق** — التحكم في رؤية النافذة الرئيسية
- **إغلاق NetGuard** — إنهاء التطبيق

---

## 🔔 الموديول 7: أيقونة علبة النظام (System Tray)

- **مؤشر نشاط ديناميكي** — نقطة خضراء عند اكتشاف حركة الشبكة، رمادية عند الخمول
- **أيقونة التطبيق الحقيقية** — تحميل من موارد Qt مع بديل HICON
- **تلميح غني** — يعرض سرعات التنزيل/الرفع الحالية
- **قائمة سياق** — إظهار NetGuard / إنهاء NetGuard
- **نقر للتبديل** — نقر مزدوج لإظهار/إخفاء النافذة الرئيسية

---

## 🎨 الموديول 8: نظام الثيمات

### الثيم الداكن (الافتراضي)
- **لوحة ألوان Zinc** — خلفية #18181B، نص #E4E4E7، حدود #3F3F46
- **لمسات زرقاء** — #3B82F6 أساسي، #60A5FA تمرير
- **لمسات زمردية** — #10B981 لمؤشرات الرفع/النجاح
- **تغطية كاملة** — QSS لكل نوع عنصر واجهة Qt

### الثيم الفاتح
- **أبيض نظيف** — خلفية #F4F4F5، نص #27272A
- **لمسات زرقاء** — #2563EB أساسي
- **تباين احترافي** — حالات التحديد والتمرير مضبوطة لسهولة القراءة

### مدير الأنماط
- **نمط Singleton** — وصول عام عبر `StyleManager::instance()`
- **إشارة الإرسال** — إشارة `themeChanged()` لتحديثات واجهة تفاعلية
- **تبديل حي** — تغيير الثيم بدون إعادة تشغيل

---

## 🔄 الموديول 9: المثبّت الذكي والمحدث الهوائي

### المثبّت الذكي
- **نشر تلقائي** — عند التشغيل لأول مرة، ينسخ نفسه إلى `C:\Program Files\NetGuard\UltimateNetGuard.exe`
- **رفع صلاحيات UAC** — يطلب تلقائياً صلاحية المسؤول عبر `ShellExecuteExW` (runas)
- **اختصار سطح المكتب** — ينشئ `NetGuard AIO.lnk` عبر واجهة `IShellLink` COM
- **أذونات الملف** — يضبط أذونات قراءة/كتابة/تنفيذ صارمة على الملف المثبت
- **تتبع الإصدار** — يكتب `version.dat` لاكتشاف إعادة التثبيت مقابل التحديث
- **إنهاء واستبدال** — عند التحديث، ينهي العملية القديمة قبل الكتابة فوق الملف
- **حوار تقدم التثبيت** — نافذة بدون حواف، قابلة للسحب، متوافقة مع الثيم

### المحدث الهوائي الصامت (OTA)
- **فحص خلفي** — يجلب ملف الإصدار عن بُعد بصمت بعد ثانيتين من بدء التشغيل
- **مقارنة إصدارات ذكية** — مقارنة `major.minor.patch` ذكية، يعمل فقط عندما الإصدار البعيد أعلى حصراً
- **حوار التحديث** — نافذة بدون حواف متوافقة مع الثيم تعرض الإصدار الجديد وسجل التغييرات
- **توجيه للموقع** — زر "حدّث الآن" يفتح صفحة التحميل الرسمية على موقع المطور

---

## 🔒 ميزات الأمان والنظام والجودة

- **نظافة الكود 100% (Clean Codebase)** — إزالة كافة التعليقات التوضيحية العربية واستبدالها بتعليقات برمجية إنجليزية معيارية
- **رفع صلاحيات المسؤول** — مانيفست UAC مع `requireAdministrator`
- **نسخة واحدة فقط (QSharedMemory)** — حارس `QSharedMemory` يمنع تشغيل نسخ متعددة؛ إذا شُغلت نسخة ثانية، يجد النافذة الموجودة عبر `FindWindowW` ويركز عليها
- **غير مدرك لـ DPI** — تخطيط متسق عبر جميع عوامل التكبير (DWM يتولى التحجيم)
- **توافق Windows 7-11** — إدخالات `supportedOS` كاملة في المانيفست
- **قاعدة جدار ناري تلقائية** — إنشاء تلقائي لقاعدة سماح واردة لنفسه
- **تصغير لعلبة النظام** — زر الإغلاق يصغر لعلبة النظام (لا يخرج)
- **إشعارات Windows مخصصة** — رسائل فقاعية في علبة النظام
- **تشغيل تلقائي مع النظام** — تسجيل اختياري في `HKCU\...\Run` للتشغيل عند بدء الويندوز

---

## 🏗️ المكدس التقني

| المكون | التقنية |
|--------|---------|
| الإطار | Qt 5.14.2 (MinGW 32-bit, بناء ثابت) |
| اللغة | C++14 |
| واجهات نظام التشغيل | WinSock2, IPHelper, WMI/COM, DirectX DXGI, PDH, Shell32, PSAPI, QtWin |
| نظام البناء | qmake |
| تحليل الحزم | هياكل بيانات سلكية مخصصة (IPv4/IPv6/TCP/UDP/ICMP) |
| نموذج البيانات | QAbstractItemModel (شجرة مخصصة) |
| الاستمرارية | JSON (السجل)، سجل Windows (الإعدادات) |
| الثيمات | QSS (داكن/فاتح) مع مدير أنماط Singleton |
